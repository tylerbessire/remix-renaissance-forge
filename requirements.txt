
# --- Web server / API ---
fastapi==0.111.0
uvicorn==0.30.0
pydantic==2.7.4
python-multipart==0.0.6
python-dotenv==1.0.1

# --- HTTP client stack: pin to avoid 'proxy/proxies' blowups ---
httpx==0.27.2
httpcore<1.0

# --- Supabase: pair known-good versions ---
supabase==2.8.1
gotrue==2.9.0

# --- Audio / DSP ---
# Keep NumPy <2 to stay compatible with librosa 0.10.*
numpy>=1.24.3,<2.0.0
scipy>=1.11.4,<1.13.0
librosa==0.10.2.post1
soundfile==0.12.1

# time-stretch / pitch-shift (needs system libs via brew install rubberband)
pyrubberband>=0.4.0

# --- ML ---
# Install torch/torchaudio per platform FIRST, then demucs
demucs==4.0.1

# --- Additional dependencies for specific services ---
requests
